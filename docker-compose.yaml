version: '3'

services:
  my_flask_app:
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: "8000"
      JWT_SECRET_KEY: "339750372817853715445184219021786970200"
    ports:
      - "8000:8000"

  db:
    image: postgres:15.4-alpine3.18
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: finance
      DB_HOST: db
    ports:
      - "5002:5432"
